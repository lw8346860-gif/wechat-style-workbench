# 公众号操作台：开发进度与接力说明

> 最后更新：2026-08-31  
> 界面版本：v-1.0.4  
> 当前主分支：`main`  
> 本轮开发前线上基线：`3b863019583b1ce13aaabac6466caaa11319bed5`

## 1. 项目定位

公众号操作台是一个面向微信公众号文章的静态富文本排版工具。核心目标是：

1. 接收普通文本、带格式富文本、剪贴板图片或外部 Agent 返回的 JSON；
2. 识别全文标题、作者、章节标题和正文等基本结构；
3. 按统一主题完成公众号文章排版与格式检查；
4. 将结果以富文本方式复制，供用户手动粘贴到微信公众号草稿箱；
5. 不依赖认证公众号 API，不直接代替用户发布文章。

## 2. 当前本地位置

- Git 项目根目录：`/Users/conan/Documents/应用开发/公众号操作台`
- 程序目录：`/Users/conan/Documents/应用开发/公众号操作台/wechat-style-prototype`
- 桌面/统一入口：`wechat-style-prototype/index.html`
- 移动入口：`wechat-style-prototype/mobile.html`
- 兼容移动预览入口：`wechat-style-prototype/mobile-preview.html`
- 公众号公开链接端侧助手：`wechat-style-prototype/wechat-link-helper.mjs`
- macOS 启动器：`wechat-style-prototype/启动公众号操作台.command`
- Windows 启动器：`wechat-style-prototype/启动公众号操作台-Windows.cmd`

项目已于 2026-08-27 从旧目录 `/Users/conan/Documents/资管风铃` 迁移到当前目录。Git 元数据、提交历史和远程配置均已迁移；旧目录中的其他无关文件没有迁入。

## 3. 线上地址与代码仓库

- 腾讯云正式站：<https://mp.togma.online/>
- GitHub Pages：<https://lw8346860-gif.github.io/wechat-style-workbench/>
- GitHub 仓库：<https://github.com/lw8346860-gif/wechat-style-workbench>
- Git 远程：`origin = https://github.com/lw8346860-gif/wechat-style-workbench.git`

GitHub Pages 由 `.github/workflows/pages.yml` 在 `main` 分支推送后自动部署。腾讯云目前是单独的静态文件部署，不会随 GitHub 自动同步。

## 4. 技术结构

当前没有框架构建步骤，也没有数据库或云端业务后端。

| 部分 | 实现方式 |
| --- | --- |
| 主界面与业务逻辑 | 单文件 `index.html`，内含 HTML、CSS、JavaScript |
| 移动版 | 同一主页面通过 `?view=mobile` 使用独立响应式布局；`mobile.html` 负责跳转 |
| 本地持久化 | 浏览器 `localStorage` |
| 公开链接读取 | Node.js 18+ 端侧助手，监听 `127.0.0.1:4318` |
| 本地静态服务 | Python HTTP Server，监听 `127.0.0.1:4317` |
| 线上托管 | GitHub Pages + 腾讯云 Nginx 静态站 |

### 浏览器本地数据键

- 自定义主题：`wechat-workbench-custom-themes-v1`
- 白天/黑夜/跟随系统：`wechat-workbench-appearance-v1`
- 桌面编辑区宽度：`wechat-workbench-editor-width-v1`
- 首次指导范文是否已关闭：`wechat-workbench-guidance-sample-dismissed-v1`

这些数据仅保存在当前浏览器、当前设备，未上传云端。清理站点数据后会丢失。

## 5. 已完成能力

### 5.1 编辑与排版

- 正文、章节标题、全文标题、作者、导语、引用和图片具有独立语义角色。
- 默认正文 16px、章节标题 32px、两端缩进 8px、段后距 24px。
- 工具栏“距”模块以 3×3 矩阵同时管理字间距、行间距和段间距的小/中/大三档。
- 字间距档位为 `0` / `0.1em` / `0.2em`，默认由原来的大档 `0.2em` 调整为中档 `0.1em`。
- 正文行间距档位为 `1.6` / `1.8` / `2.0`，默认中档；标题同步采用更紧凑的 `1.2` / `1.35` / `1.5`。
- 段间距档位为 `16px` / `24px` / `32px`，默认中档。
- “距”在有选区时作用于所选自然段，没有选区时作用于全文；自定义主题会同时保存三个间距档位。
- 正文字号可选 14/15/16，标题字号可选 24/32。
- 字体可选默认、黑体、宋体、楷体。
- 支持主题色、主题色粗体、斜体、引用、两端缩进 8/16、左/中/右对齐。
- 工具命令对框选内容生效；没有文本选区时，正文/标题字号及章节序号模式作为全文设置。
- 支持 10 步撤回和 10 步反撤回。
- 桌面编辑框可调整宽度，工具栏在长文滚动时保持可用。

### 5.2 章节标题与序号

目前支持五种全文章节标题模式：

1. `1. 2. 3.`；
2. `一、二、三`；
3. 中文序号另起一行，下一行显示标题；
4. 中文序号另起一行，标题使用主题色背景框和白字；
5. 无序号。

默认使用第 4 种。切换序号模式会重新处理全文章节标题，并避免在原文已有序号时重复添加序号。第 4 种标题背景只包裹实际文字宽度，不占满整行。

### 5.3 基础结构识别

- 第一条符合条件的无序号短句可识别为全文标题，不自动添加章节序号。
- `作者：xx`、`撰文：xx`、`执笔：xx` 等识别为作者。
- 带“一、”“1.”“第一章”等编号且没有正文句末标点的短段落识别为章节标题。
- 没有标点的短句可以保守识别为章节标题。
- 普通段落识别为正文。
- “摘要”“导语”等标签不自动套引用，按正文风格处理。
- 自动排版会保留原来的自然段边界，同时删除空段，因为版式已有固定段间距。

基础识别是本地规则，不具备完整语义理解。复杂文章仍建议使用外部 Agent JSON 流程。

### 5.4 外部 Agent JSON

页面内置 Agent 提示词，要求 Agent 返回英文半角 JSON：

```json
{
  "article_title": "全文标题",
  "author": "作者姓名",
  "intro": ["导语自然段"],
  "sections": [
    {
      "title": "一、章节标题",
      "paragraphs": [
        {
          "text": "完整正文自然段",
          "highlights": ["完整重点句。"]
        }
      ]
    }
  ]
}
```

现有导入器能够修复一部分中文弯引号、全角冒号/逗号和相似标点造成的非标准 JSON。重点句约束为：

- 必须按句号、问号或叹号为边界保留完整句子，不能在逗号处分断；
- 每章最多 3 句，通常 1—2 句；
- 尽量不选章节标题正上方上一段末句或正下方第一句；
- 导语和普通开场正文不得自动设为引用；
- 引用主要留给有明确说话者或来源、确实重要的原话，并由用户手动决定。

### 5.5 自动排版与标点清洗

“自动排版并检查”的主要处理顺序：

1. 清理 Markdown 强调标记；
2. 将粘贴内容规范为顶层自然段，保留段落边界；
3. 将半角或相似双引号规范为真正的中文左、右双引号 `“ ”`；
4. 清除孤立、重复的章节序号；
5. 检测全文标题、作者、章节标题和正文；
6. 删除空自然段；
7. 应用当前主题、字号、字体、缩进和序号；
8. 运行格式检查。

当前可转换的双引号来源包括 ASCII `"`、全角 `＂`、双撇号 `″`、低位/高位变体 `„ ‟`。数字尺寸中的 `16"`、`27″` 会保留，避免误改英寸符号。

### 5.6 格式检查

- 对正文、章节标题和加粗重点句分别检查字体、字号与外观。
- 采用“多数压倒少数”：以字符权重统计出的主流格式为基准，少数格式判为偏差。
- 检查主题色粗体、两端缩进和段落间距的一致性。
- 异常位置以不进入复制结果的波浪线/提示标记显示。
- 点击具体检查项可以定位到编辑区中的异常位置。
- 应用主题后会自动排版并再次检查。

### 5.7 主题与外观

- 内置主题顺序：阅读蓝、杂志紫、经典橘。
- 主题色会同步影响章节标题、标题色块、加粗重点句和相关界面组件。
- 自定义主题数量不设上限，可保存、应用、修改和删除。
- 自定义主题使用自适应宽度的主题色标签排列，仅存当前浏览器。
- 支持白天、黑夜、跟随系统三种模式；深色模式会调整编辑区正文对照色，主题色保持不变。

### 5.8 搜索、引用、图片及其他组件

- 桌面端支持类似 Word 的全文搜索、上一个/下一个结果和结果计数，点击外部可关闭。
- 引用按钮可在引用/非引用之间切换；导语默认不是引用。
- 引用默认左侧主题色竖线；示例重要引语可使用斜体、粗体和主题色。
- 可直接从剪贴板粘贴图片，图片自动居中。
- 点选图片后可用 `Command+C` / `Ctrl+C` 单独复制；复制整篇富文本也保留图片与居中样式。
- 空白滚动批注框会清洗粘贴格式，输出 12px、最小行距、无段距、无字距的可滚动批注组件。
- 首次打开显示位于正文画布内的新手指引；用户修改或删除后，之后刷新保持空白编辑器。

### 5.9 桌面与移动端

- 默认根据设备能力和屏幕条件自动进入桌面或移动布局。
- 可用 `?view=desktop` 或 `?view=mobile` 显式覆盖。
- `mobile.html` 是真实移动网页入口，不是仅用于展示的手机模拟图片。
- 桌面端手机图标悬停可显示发布效果预览，点击进入移动版。
- 移动端具有独立紧凑布局、移动工具栏及进入 PC 完整版入口。

## 6. 公众号公开链接学习

静态网页本身受浏览器跨域和微信页面限制，不能直接可靠读取 `mp.weixin.qq.com`。当前采用端侧助手：

1. 用户在“学习新格式”框中只粘贴一个公开公众号文章链接；
2. 页面请求本机 `http://127.0.0.1:4318/api/learn-wechat-style`；
3. `wechat-link-helper.mjs` 验证域名、下载公开文章、提取正文并清洗危险标签；
4. 页面将正文填入样稿区，再执行版式学习与应用。

约束：

- 仅允许 `https://mp.weixin.qq.com` 的公开文章；
- 需要用户电脑安装 Node.js 18+ 并运行端侧助手；
- GitHub Pages 和腾讯云静态站不会替用户运行该助手；
- iOS/Android 手机浏览器通常没有这一 Node 端侧环境，应改为粘贴带格式全文；
- 微信页面若要求验证、链接失效或页面结构变化，读取可能失败。

端侧助手包含 `--self-test`，但真实公众号页面仍需人工抽样验证。

## 7. 本地启动与日志

### macOS

双击：

`wechat-style-prototype/启动公众号操作台.command`

该脚本会按需启动：

- 静态页面：`http://127.0.0.1:4317/index.html?view=desktop`
- 链接读取助手健康检查：`http://127.0.0.1:4318/health`

### Windows

安装 Node.js 18+ 后双击：

`wechat-style-prototype/启动公众号操作台-Windows.cmd`

### 本地日志

- 静态服务：`/tmp/wechat-style-static-server.log`
- 链接读取助手：`/tmp/wechat-style-link-helper.log`

这是静态项目，没有应用日志框架。上述日志由 macOS 启动脚本生成；如果直接用其他命令启动，日志位置取决于启动方式。

## 8. 腾讯云部署信息

- Nginx 网站根目录：`/var/www/mp.togma.online`
- Nginx 访问日志：`/var/log/nginx/access.log`
- Nginx 错误日志：`/var/log/nginx/error.log`
- 历史版本备份：`/var/www/releases/mp-togma-*`
- 当前本机可用 SSH 别名：`invest-animal-tencent`

部署时只同步：

- `index.html`
- `mobile.html`
- `mobile-preview.html`

每次覆盖腾讯云文件前应先把现有站点完整备份到带时间和提交号的 `/var/www/releases/mp-togma-*` 目录，并在替换后运行 `nginx -t`。禁止把 API 密钥、SSH 私钥或下载目录中的凭据提交到 Git。

## 9. 当前验证状态

- `main` 与 `origin/main` 当前一致。
- 上一线上提交：`3b86301`（扩展中文双引号转换范围）。
- 该提交在 2026-08-24 已同步 GitHub Pages 和腾讯云，两个公开页面当时与本地 `index.html` 的 SHA-256 一致。
- 2026-08-27 只进行了本地目录迁移，没有修改运行源码，因此线上内容未发生变化。
- 2026-08-31 已完成“距”3×3 间距模块开发；已验证默认三项中档、全文与框选局部切换、主题保存/恢复、复制 HTML 输出以及 390px 移动端工具栏可见性。发布提交号以本文件之后的最新 `git log` 为准。

重要最近提交：

- `3b86301` 扩展中文双引号转换范围
- `bb88b24` 自动转换中英文双引号
- `c7f3092` 增加空段清理与默认字距
- `a8ac07a` 保留粘贴文章的自然段结构
- `a7300b8` 区分全文标题与章节标题
- `2a54457` 避免自动排版重复添加章节序号
- `9635c6d` 修复自动排版的章节标题识别
- `1698f7e` 增加图片粘贴复制与短标题识别
- `218b6e4` 增加本地基础结构识别

## 10. 已知限制与风险点

1. 主页面是约 20 万字节的单文件应用，继续扩展时维护成本会逐渐上升；未经用户确认不要贸然重构为框架项目。
2. `document.execCommand('copy')` 是兼容性方案，未来浏览器可能进一步限制；每次涉及复制逻辑都需在 Chrome、Edge 和 Safari 中抽样。
3. 微信公众号编辑器可能清洗部分 HTML/CSS。最终验收标准是粘贴进微信草稿箱后的实际表现，而不是仅看本工具预览。
4. 图片复制受浏览器剪贴板权限、图片来源 CORS 和微信编辑器清洗影响。
5. 基础结构识别是启发式规则，短句、表格、诗歌、列表或没有明显标题的文章可能误判。
6. 公开链接提取依赖微信已发布页面的 DOM；微信改版后可能需要更新提取器。
7. 自定义主题没有云同步或账号隔离，换浏览器或清理数据后不会保留。
8. 手机端可完成静态排版操作，但公开链接自动读取通常需要桌面端侧助手。

## 11. 接力 Agent 的工作约定

1. 开始前先阅读本文件、`README.md`、`git status` 和最近提交。
2. 以 `/Users/conan/Documents/应用开发/公众号操作台` 为唯一项目根目录，不再使用旧的 `/Users/conan/Documents/资管风铃/wechat-style-prototype`。
3. 保持当前静态架构和相对路径，除非用户明确同意重构。
4. 不覆盖用户无关文件，不使用破坏性 Git 操作。
5. 任何排版改动先在本地实现和测试；用户确认通过后，再同步 GitHub 与腾讯云。
6. 提交时只包含本次任务涉及的文件，先运行 `git diff --check`。
7. 修改链接读取器后至少运行：

   ```bash
   node wechat-style-prototype/wechat-link-helper.mjs --self-test
   ```

8. 涉及编辑器功能时至少人工验证：
   - 普通文本粘贴是否保留自然段；
   - 自动排版是否清空段但不合并正常段落；
   - 全文标题是否不被追加章节序号；
   - 已有章节序号是否不会重复；
   - 主题、序号、正文和标题全局命令是否生效；
   - 加粗、斜体、引用能否再次点击取消；
   - JSON 导入是否兼容标准标点和常见中文/全角标点；
   - 富文本复制到微信草稿箱后的字号、段距、缩进、主题色和图片是否保持。
9. 上线后应分别检查 GitHub Pages 与腾讯云公开 URL，并确认实际页面包含本次变更。
10. 不把任何云端密钥、Cookie、登录态或本机私钥写入代码、日志、本文档或提交记录。

## 12. 产品方向尚未正式完成的部分

以下属于后续可讨论方向，不应被误认为已经具备：

- 不依赖端侧助手、直接由纯静态网页读取任意公众号链接；
- 直接写入未认证公众号草稿箱或自动发布；
- 跨设备同步用户主题；
- 云端用户账号、多人协作或文章数据库；
- 完全替代 Agent 的深层语义判断；
- 对微信公众号编辑器所有未来版本的样式兼容保证。
